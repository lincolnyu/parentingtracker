<UserControl
    x:Class="ParentingTrackerApp.Views.TrackingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ParentingTrackerApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Base.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height=".3*" MinHeight="150"/>
            <RowDefinition Height=".2*" MinHeight="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height=".5*" MinHeight="60"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock>Event type:</TextBlock>
                    <ComboBox ItemsSource="{Binding EventTypes}" SelectedItem="{Binding SelectedEventType, Mode=TwoWay}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name, Mode=OneWay}" 
                                   Foreground="{Binding Color, Converter={StaticResource ColorToBrushConverter}}"></TextBlock>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
            </Grid>
            <TextBlock>Notes</TextBlock>
            <TextBox Text="{Binding Notes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"></ColumnDefinition>
                    <ColumnDefinition MaxWidth="300"></ColumnDefinition>
                    <ColumnDefinition MaxWidth="300"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Start" Grid.Row="0" Grid.Column="0"></TextBlock>
                <DatePicker Grid.Row="0" Grid.Column="1" IsEnabled="{Binding StartPickerEnabled}"
                            Date="{Binding StartDate, Mode=TwoWay}"></DatePicker>
                <TimePicker Grid.Row="0" Grid.Column="2" IsEnabled="{Binding StartPickerEnabled}"
                            Time="{Binding StartTimeOfDay, Mode=TwoWay}"></TimePicker>
                <TextBlock Text="End" Grid.Row="1" Grid.Column="0"></TextBlock>
                <DatePicker Grid.Row="1" Grid.Column="1" IsEnabled="{Binding EndPickerEnabled}"
                            Date="{Binding EndDate, Mode=TwoWay}"></DatePicker>
                <TimePicker Grid.Row="1" Grid.Column="2" IsEnabled="{Binding EndPickerEnabled}"
                            Time="{Binding EndTimeOfDay, Mode=TwoWay}"></TimePicker>
            </Grid>
        </StackPanel>
        <TextBlock Grid.Row="1" Grid.Column="0">Running:</TextBlock>
        <ListView Grid.Row="1" Grid.Column="1"
                  ItemsSource="{Binding RunningEvents}" 
                  SelectedItem="{Binding SelectedRunningEvent, Mode=TwoWay}"
                  SelectionMode="Single"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Title}"></TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal">
            <Button Click="NewOnClick">New</Button>
            <Button Click="StartOnClick">Start</Button>
            <Button IsEnabled="{Binding CanStop}" Click="StopOnClick">Stop</Button>
            <Button Click="LogOnClick">Just Add</Button>
        </StackPanel>
        <TextBlock Grid.Row="3" Grid.Column="0">Logged:</TextBlock>
        <ListView Grid.Row="3" Grid.Column="1"
                  ItemsSource="{Binding LoggedEvents}"
                  SelectedItem="{Binding SelectedLoggedEvent, Mode=TwoWay}"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding LocalisedTimeRange}" Margin="5,0,5,0"></TextBlock>
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding Type}" Margin="5,0,5,0"></TextBlock>
                        <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding Notes}" Margin="5,0,5,0"></TextBlock>
                        <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Orientation="Horizontal">
                            <ToggleButton Name="EditingButton" Content="&#xE104;" FontFamily="Segoe UI Symbol"
                                          IsChecked="{Binding IsEditing, Mode=TwoWay}"></ToggleButton>
                            <Button Content="&#xE106;" FontFamily="Segoe UI Symbol"
                                          Click="RemoveLoggedOnClick" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}"></Button>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
