<Page
    x:Class="ParentingTrackerApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ParentingTrackerApp"
    xmlns:converters="using:ParentingTrackerApp.Converters"
    xmlns:views="using:ParentingTrackerApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Base.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock Text="{Binding Time, Converter={StaticResource DateTimeToStringConverter}}"></TextBlock>
            <Pivot>
                <!--
                <Pivot.RightHeader>
                    <CommandBar ClosedDisplayMode="Compact">
                        <AppBarButton Icon="Back" Label="Previous"/>
                        <AppBarButton Icon="Forward" Label="Next"/>
                    </CommandBar>
                </Pivot.RightHeader>
                -->
                <PivotItem Header="Track">
                    <views:TrackingView/>
                </PivotItem>
                <PivotItem Header="Customize">
                    <views:CustomizingView/>
                </PivotItem>
                <PivotItem Header="Summary">

                </PivotItem>
            </Pivot>
          
        </StackPanel>
    </Grid>
</Page>
